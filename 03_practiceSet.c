#include <stdio.h>
int main()
{
    // Write a program to determine whether a student has passed or failed. To pass, a student requires a total of 40% and at least 33% in each subject. Assume there are three subjects and take the marks as input from the user.

    // int sub1, sub2, sub3;
    // printf("enter the marks of 3 subject \n");
    // scanf("%d  %d  %d", &sub1, &sub2, &sub3);
    // int total = sub1 + sub2 + sub3;
    // float percentage = (total / 300) * 100;
    // if (sub1 >= 33 && sub2 >= 33 && sub3 >= 33 && total >= 40)
    // {
    //     printf("student is pass ");
    // }
    // else
    // {
    //     printf("student is fail ");
    // }
    // return 0;

    // Calculate income tax paid by an employee to the government as per the slabs mentioned below: Income Slab                       2.5 – 5.0L                               5.0L - 10.0L                          Above 10.0L                         Tax  5% 20% 30%

    //     int salary   ;
    //     float tax  ;
    //     printf("Enter the salary \n ") ;
    //     scanf("%d" , &salary ) ;
    //     if(salary>250000 && salary<500000 ){
    //         tax = salary * (5.0/100) ;
    //         printf("%.2f itna tax hai bhai tera "  , tax );
    //     }
    //     else if(salary>500000 && salary<1000000 ){
    //         tax = salary * (10.0/100) ;
    //          printf("%.2f itna tax hai bhai tera "  , tax );
    //     }
    //     else if(salary>1000000 ){
    //         tax = salary * (20.0/100) ;
    //          printf("%.2f itna tax hai bhai tera "  , tax );
    //     }
    //     else{
    //         printf("no tax for you ");
    //     }

    //     return 0 ;

    // Write a program to find whether a year entered by the user is a leap year or not. Take year as an input from the user.

    // int year ;
    // printf("Enter the year you want to check\n") ;
    // scanf("%d" , &year ) ;
    // if((year%4==0 && year%100!= 0 ) || year %400 == 0  ) {
    //     printf("%d is leap year " , year ) ;
    // }
    // else {
    //     printf("%d is not leap year " , year );
    // }
    // return 0 ;

    // }
    // Write a program to determine whether a character entered by the user is lowercase or not.

    // char ch ;
    // printf("Enter the char\n") ;
    // scanf("%c" , &ch) ;
    // if(ch>='a' && ch<= 'z') {
    //     printf("%c is lowercase" , ch );

    // }
    // else{
    //     printf("%c is not lowercase " , ch );
    // }
    //  return 0 ;

    // }
    // Write a program to find greatest of four numbers entered by the user.

//     int a, b, c, d;
//     printf("Enter the four numbers :\n");
//     scanf("%d %d %d %d", &a, &b, &c, &d);
//     if (scanf("%d %d %d %d", &a, &b, &c, &d) != 4)
//     {
//         printf("Invalid input! Please enter four integers.\n");
//         return 1; // Exit the program with an error code
//     }
//     if (a > b && a > c && a > d)
//     {
//         printf("%d is greatest number from the numbers you entered ", a);
//     }
//     else if (b > a && b > c && b > d)
//     {
//         printf("%d is greatest number from the numbers you entered ", b);
//     }
//     else if (c > a && c > b && c > d)
//     {
//         printf("%d is greatest number from the numbers you entered ", c);
//     }
//     else if (d > a && d > b && d > c)
//     {
//         printf("%d is greatest number from the numbers you entered ", d);
//     }
//     return 0;
// }
